# TypeScript Tutorials & Practice

This repository contains structured TypeScript learning material, covering both **core fundamentals** and **advanced concepts**, along with practice problems.

It’s organized for step-by-step learning and hands-on experimentation.

---

## 📁 Folder Structure

### `/ts`
Main TypeScript source files:

- `index.ts` – Entry point / basic examples  
- `function.ts` – Functions, typing parameters & returns  
- `classes.ts` – Classes, interfaces, access modifiers  
- `generics.ts` – Generic functions, classes, constraints  
- `asyncTS.ts` – Async/await, Promises, async behavior  
- `decorators.ts` – Decorator basics and examples  
- `literal_types.ts` – Literal & union types  
- `type_guard.ts` – Type guards and narrowing  
- `practice_problems_set1.ts` – Practice problems (Set 1)  
- `practice_problems_set2.ts` – Practice problems (Set 2)

### Other folders

- `/js` – Compiled JavaScript output  
- `/dist` – Build output  
- `.vscode` – VS Code settings

---

## 🚀 Getting Started

### 1. Install TypeScript

```bash
npm install -g typescript
